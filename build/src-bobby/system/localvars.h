 AccelData _acc;
 int blockTickRunning ;
 int accelReady;
 byte isLeader ;
 byte electing ;
 Timeout leaderElectionTimeOut;
 syncData_t maxSystemClock;
 uint16_t minId;
 byte parent;
 uint16_t STlevel ;
 byte nbNeededAnswers;
 byte step;
 byte candidate;
 byte role_election;
 uint16_t gradient;
 uint16_t prev_gradient;
 uint16_t distance;
 uint16_t distFromB;
 uint16_t distFromC;
 uint16_t remainingIds[2];
 byte waiting[NUM_PORTS];
 byte children[NUM_PORTS];
 uint16_t minGradient;
 uint16_t maxDistance;
 Neighborhood myNeighborhood;
 long int offset ;
 Time localClockMaxReach ;
 Timer syncTimer;
 long unsigned int syncRound ;
 long unsigned int numSync ;
 PRef syncChildren[NUM_PORTS];
 syncData_t syncData[NUM_SYNC_DATA];
 double speedAvg ;
 byte numRequestedAnswers ;
 Time testTraversalGo ;
 Chunk clockChunkPool[NUM_CLOCK_CHUNK];
 byte countChildren;
 byte numCoord ;
 signed char coord[30][4];
 signed char xCoord ;
 signed char yCoord ;
 signed char zCoord ;
 byte haveCoor ;
 byte alreadyShared ;
 Timeout propagateCoord;
 Timeout logTimeout;
 int numMsg ;
 Chunk coordChunks[NUM_COORD_CHUNKS];
 ReceivePacketQueue globalRq;
 Chunk* thisChunk;
 int sentNum ;
 Neighborhood thisNeighborhood;
 Timer ttNeighbor[6];
 SystemHandler system;
 Timeout* thisTimeout;
 Timeout* timeoutList;
 Timer* timerList;
 int maxTimeouts ;
 byte 		currentRGB[3];
 Color 	currentColor;
 Intensity 	currentIntensity ;
 byte PCConnection ;
 PRef toHost ;
 byte seq ;
 Chunk logChunkPool[NUM_LOG_CHUNK];
 byte numFreeChunks;
 char allocated;
 Chunk rxChunks[NUM_RXCHUNKS];
 Chunk txChunks[NUM_TXCHUNKS];
 blockConf EEMEM nv_conf;
 blockConf conf;
 MicData _mic;
 Port port[NUM_PORTS];
 int debugmode ;
 SpanningTree* trees[MAX_SPANTREE_ID];
 int maxSpanId ;
 Timeout nryTimeout;
 byte haveTimeout ;
 GenericHandler oldNbrChgHander;
 byte collectedCountChildren ;
 byte collectedCount ;
 uint16_t rootId;
 byte gotmsg ;
 int stcounter ;
 byte ignoredHandshakeCount[NUM_PORTS];
